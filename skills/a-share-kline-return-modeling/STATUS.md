# Status

## 2026-05-29

- Completed a feature-gap analysis for the current compact60 baseline on 2026-03-02 through 2026-04-30.
- Saved analysis tables under `outputs/feature_gap_analysis_202603_202604/` and wrote the summary report to `docs/feature_gap_analysis_202603_202604.md`.
- Main finding: the first-layer stock model has a recall problem. Among 782 actual `future_5_return >= 10%` opportunities, only 105 entered the Top20 candidate pool and only 28 reached Top3.
- Top20 reranking still has replacement room, but it is not the root cause: 14 trading days had at least 3 `>=10%` names inside Top20, while Top3 did not select all of them.
- March failure mode: selected losses look like weak-market rebound traps. In March, selected `<=-5%` losses had median `market_avg_pct_chg` `+0.0584` and `industry_up_ratio` `0.8889`, while selected `>=10%` winners had median `market_avg_pct_chg` `-1.6580` and `industry_up_ratio` `0.1111`.
- Missed winners are often quieter before takeoff than selected winners: lower turnover, lower same-day return, lower 60-day position, and weaker market/industry ranks.
- Current recommendation: do not add another post-model market gate. Test one small feature replacement experiment inside the first-layer stock model: raw market state, weak-market reversal interactions, rebound-trap interactions, and quiet-breakout features, while keeping daily fixed Top3.
- Tested that small feature replacement direction without changing the production script. Adding 14 raw market/weak-market/quiet-breakout interactions improved the same-code baseline from Top3 `>=10%` 24/129 to 26/129 and reduced losses from 29/129 to 28/129, but still did not beat the historical compact60 baseline of 28/129. Replacing old features to keep 60 features generally worsened results and increased losses.
- Ran a coarse setup-family diagnosis on 2025-10 through 2026-04 and saved outputs under `outputs/setup_discovery_202510_202604/`. High-upside shape families are also high-loss families: for example `high_pos_terminal` had `>=10%` rate 20.77% but `<=-5%` rate 31.32%. Low-risk quiet reversal families had much lower `>=10%` rates.
- Probed the existing `概念题材` data from `00_股票清单.csv` and saved outputs under `outputs/theme_feature_probe_202510_202604/`. Theme heat has usable lift but is not a standalone selector: in 2026-03 through 2026-04, the top bin of `max_theme_avg_turnover_rank` had `>=10%` rate 11.69% vs 4.75% in the bottom bin, while loss risk also rose.
- Wrote the direction reset to `docs/model_direction_reset_20260529.md`. New recommendation: stop micro-tuning the single stock K-line classifier and build a theme/industry/stock three-layer feature table, then keep daily fixed Top3 and validate against the same 2026-03 to 2026-04 baseline.
- Added pool-internal theme features to `scripts/00_clean_data.py`. The feature table now uses existing `概念题材` metadata and same-day/past K-line fields to produce 24 leakage-safe theme columns such as `max_theme_up_ratio`, `max_theme_avg_turnover_pct`, `max_theme_ret_5_ge05_ratio`, theme ranks, and stock-vs-theme relative strength fields.
- Re-ran cleaning. `data/个股k线特征数据.csv` now has 72,531 rows, 228 stocks, and 146 columns. Theme feature non-null coverage is about 94% for same-day/5-day fields and about 88% for 20-day relative fields.
- Smoke-tested `scripts/01_predict_stock_direction.py` on 2026-04-23 after adding theme columns. The existing compact60 model path still runs and ignores the new columns until the feature list is intentionally updated.
- Tested theme features inside the stock model on 2026-03-02 through 2026-04-30 and saved outputs under `outputs/theme_model_experiment_202603_202604/`; summary written to `docs/theme_model_experiment_202603_202604.md`.
- Result: direct theme-feature insertion did not beat the compact60 baseline. Baseline Top3 was `>=10%` 24/129 and `<=-5%` 29/129; `add_theme_all84` was `>=10%` 23/129 and `<=-5%` 33/129; `add_theme_core72` and `replace12_theme_core60` were worse.
- Conclusion: keep theme fields in the cleaned data layer, but do not add them to the production feature list yet. The next direction should learn theme-state regimes such as startup, acceleration, crowded, and fading, instead of treating theme heat as ordinary flat numeric features.
- Added a market-correction/action decision branch to `scripts/03_apply_signal_decision_layer.py` and tested it on the March 2026 main-model rerun. It classifies daily market opportunity into A/B/C/D/E, applies market/candidate risk score adjustments, and outputs `Top3/Top2/Top1/不出手`.
- March 2026 result was not acceptable as an optimization: final action layer traded only 11 stocks across 7 dates, with `>=10%` 2/11, `<=-5%` 4/11, and average return -1.46%. It avoided many bad days but also skipped several profitable days such as 2026-03-18, 2026-03-20, 2026-03-23, and 2026-03-26.
- Saved March market-action details under `outputs/final_signal_market_action_202603/2026年3月市场纠正与是否出手明细.md`. Current conclusion: market action gating is useful for diagnostics, but this version should not be treated as a successful production improvement.
- Generated a March 2026 failure attribution report under `outputs/failure_attribution_202603/2026年3月失败归因报告.md`.
- Attribution finding: March had 234 actual `future_5_return >= 10%` opportunities in the stock pool, but only 33 entered the Top20 candidate pool and only 7 reached Top3. The first-layer recall problem is much larger than the Top3 reranking problem.
- Median profile of actual March `>=10%` winners: same-day `pct_chg` -0.68%, turnover 2.50%, `ret_5` -2.64%, `ret_20` -4.27%, `range_pos_20` 0.276, `range_pos_60` 0.335. Most missed winners are low-position/low-heat weak-market recovery candidates.
- Median profile of model Top3 `<=-5%` losses: `ret_20` +9.88%, `range_pos_20` 0.505, `range_pos_60` 0.670, theme turnover rank 0.688, industry average pct change +0.17%. The model is often selecting hotter rebound/continuation names in a weak market.
- Current conclusion: the compact60 explosive-return model is structurally biased toward high-heat/high-position candidates, while many March winners come from quiet low-position recovery. This cannot be solved by a post-model signal layer alone.

## 2026-05-26

- Rebuilt the skill workspace from scratch.
- Step 1 is data cleaning and data model design.
- Raw input source is `skills/a-share-data-fetching/data/`.
- All 5-day modeling scripts live under `skills/a-share-kline-return-modeling/scripts/`.
- Clean outputs will be written directly to `skills/a-share-kline-return-modeling/data/`.
- Cleaning output is intentionally limited to two tables: `个股k线特征数据.csv` and `指数k线特征数据.csv`.
- Added the first stock-level 5-day direction prediction script: `scripts/01_predict_stock_direction.py`.
- Stock prediction now uses `00_股票清单.csv` plus a date range and writes one result CSV.
- Added the market-level 5-day risk prediction script: `scripts/02_predict_market_risk.py`.
- Removed the standalone holiday-window core script; holiday features remain in cleaning and are used by the final signal layer.

## 2026-05-28

- Re-scoped the skill goal to daily actionable Top3 selection, targeting stocks with future 5-trading-day return `>= 10%`.
- Added training design docs:
  - `docs/top3_10pct_feature_table_design.md`
  - `docs/top3_10pct_training_plan.md`
  - `docs/top3_10pct_training_workflow.md`
- Added labels to the stock feature table:
  - `label_5d_ge10`
  - `label_5d_ge05`
  - `label_5d_loss05`
  - `daily_future_return_rank`
  - `daily_future_return_top3_label`
- Changed the default stock prediction objective to `return_threshold` with `--label-up-threshold 0.10`.
- Added `scripts/02c_predict_stock_loss_risk.py` for future 5-day loss risk (`future_5_return <= -5%`).
- Updated the final signal layer to optionally read stock loss-risk predictions via `--stock-loss-risk-prediction`.
- Ran a one-day smoke test for 2026-03-02; the small loop produced no final trade because all Top3 10% candidates were filtered by high 5-day loss risk.
- Learned the focused dates `2026-01-05`, `2026-03-23`, `2026-04-02`, `2026-04-03`, `2026-04-07`, `2026-04-13`, `2026-04-28`, `2026-04-29`, and `2026-04-30`.
- Added positive selection features to the stock table, including 2-day return, 3-day volume/amount ratios, 60-day range position, breakout flags, pullback setup score, momentum continuation score, industry/market rank features, and market/industry 5-day strength ratios.
- Added `--enable-positive-setup-rerank` to the stock prediction script. On the focused 9-date learning set, the 10% model with positive reranking improved Top3 `>=10%` hits from 8 to 14 out of 27 and improved average Top3 5-day return from 5.45% to 12.63%, without relying on the final risk/no-trade layer.
- Learned the second focused date batch: `2026-01-06`, `2026-01-07`, `2026-01-08`, `2026-01-14`, `2026-01-15`, `2026-01-16`, `2026-02-13`, `2026-03-03`, `2026-03-04`, `2026-03-31`, `2026-04-09`, `2026-04-10`, `2026-04-15`, `2026-04-24`, `2026-04-27`, and `2026-05-06`.
- On the second batch, the base 10% model outperformed positive reranking: base Top3 had 12 `>=10%` hits out of 48 with average Top3 return 5.71%, while positive reranking had 5 hits and average Top3 return 2.99%.
- Conclusion after the second batch: positive setup reranking is not stable enough for the default main loop. Keep it as an optional research switch, and use the base `>=10%` return-threshold model as the current first-layer default.
- Analyzed the second-batch Top3 `<= -5%` cases. The common failure mode is not random downside; the first layer over-scores high-position/overextended momentum, strong 20-day winners, and failed strong pullbacks.
- Added terminal/overextended momentum features and heavier negative sample weights for terminal-loss examples. Also added optional `--enable-terminal-risk-penalty`, which subtracts `0.15 * overextended_momentum_score` from the first-layer ranking score.
- On the second batch, terminal penalty reduced Top3 `<= -5%` picks from 5 to 2, with Top3 `>=10%` hits moving from 12 to 11 and average Top3 return from 5.71% to 5.90%. On the first batch it reduced hit quality, so it remains an optional research/defensive switch rather than the default main loop.
- Learned the 2026-04-23 single-day failure case. The first-layer model was over-scoring small-sample industry rank signals and raw industry index point levels.
- Added small-industry rank protection: when an industry has fewer than 5 stocks on a date, stock-vs-industry rank features fall back to market rank instead of treating the stock as a false industry leader.
- Excluded raw `industry_index_close` from model features; relative/return industry index features remain available.
- Added stronger sample weights for true explosive winners (`future_5_return >= 15%` and `>= 20%`) and for short-term weak `<= -5%` loss cases.
- On 2026-04-23 with the corrected 10% objective, Top1 improved to `002655 共达电声` with actual 5-day return `27.56%`; Top3 improved from 0/3 `>=10%` to 1/3, and Top3 average return moved from about 0.02% to 11.01%.
- Compared 10% vs 20% return-threshold objectives on 2026-04-15 through 2026-04-30. The 20% objective is a better Top3 explosion detector for the 10% business target on this period: Top3 `>=10%` hits improved from 5/36 to 17/36, Top3 average return improved from 4.14% to 7.58%, while `<= -5%` picks stayed at 4/36.
- Current working hypothesis: the first layer should use an explosive-return objective such as `--label-up-threshold 0.20` to rank the strongest Top3 candidates, while evaluation and business acceptance still use `label_5d_ge10`.
- Analyzed the 4 loss picks from the 2026-04-15 to 2026-04-30 explosive objective run: `002902 铭普光磁`, `002580 圣阳股份`, `002842 翔鹭钨业`, and `001229 魅视科技`.
- Loss-pick common mode: not weak/quiet stocks, but over-hot continuation candidates. Compared with same-period Top3 winners, these loss picks had much higher average turnover, higher 10/20-day returns, higher range position, higher overextended/terminal-chase scores, and higher market turnover/rank heat.
- Upgraded `--enable-terminal-risk-penalty` from a simple `0.15 * overextended_momentum_score` penalty to a composite terminal-chase ranking penalty using overextended momentum, terminal-chase score, pullback-failure score, market ret-5/ret-20 ranks, turnover rank, and 20-day range position.
- On 2026-04-15 through 2026-04-30 with the 20% explosive objective, the composite terminal penalty kept Top3 `>=10%` hits at 17/36, reduced `<= -5%` picks from 4/36 to 3/36, and improved average Top3 5-day return from 7.58% to 9.20%.
- Continued learning the remaining loss cases. The stable incremental rules were extreme-turnover high-position closes and weak-midterm short-term pop patterns. Added those to the composite terminal penalty and deliberately skipped a high-turnover-stall penalty because it reduced Top3 hit quality in the 4月下半月 sample.
- On 2026-04-15 through 2026-04-30 with the 20% explosive objective and updated terminal penalty, Top3 `>=10%` stayed at 17/36, `<= -5%` picks dropped from 3/36 to 1/36 versus the previous terminal version, and average Top3 return improved from 9.20% to 9.56%.
- The only remaining `<= -5%` Top3 pick in this slice is `2026-04-20 920078 族兴新材 -9.44%`; next learning target is that lower-rank replacement-loss pattern.
- Added `--feature-set` options `compact40`, `compact60`, `compact80`, and `all`, plus `--model-preset` options `baseline`, `regularized`, `deep`, and `no_balanced` to make feature-count and LightGBM parameter tuning reproducible from the command line.
- Tested feature-count and parameter presets on 2026-04-15 through 2026-04-30 using the 20% explosive objective and terminal-risk penalty. The 40-feature set could not recover the full-model quality through parameter tuning: best 40-feature hit count was `13/36` with `6/36` loss picks.
- The best current small-loop result is `compact60 + baseline`: 60 features, Top3 `>=10%` hits `19/36`, Top3 `>=5%` hits `22/36`, `<=-5%` loss picks `2/36`, average Top3 5-day return `10.41%`, median `11.34%`.
- For comparison, the prior 130-feature full model on the same slice had Top3 `>=10%` hits `17/36`, Top3 `>=5%` hits `21/36`, `<=-5%` loss picks `1/36`, average return `9.56%`, median `9.24%`.
- Current recommendation: do not force the feature count down to 40. Use `compact60` as the only main-script feature set, and validate it on other months before treating it as stable out-of-sample performance.
- Simplified `scripts/01_predict_stock_direction.py` back to a single production path. Removed the command-line switches for feature set, model preset, target mode, positive rerank, overheat penalty, and alternate ranking models from the main script. The script now fixes the current best setup directly: 20% explosive-return label, compact60 features, baseline LightGBM, terminal-risk penalty, ST exclusion, and Top3 output.
- Tested layered loss weighting for `future_5_return <= -10%` on top of the existing `<= -5%` loss weighting. Weights 8.0, 5.0, and 3.5 all reduced Top3 hit quality and did not beat the current compact60 baseline on the 2026-04-15 to 2026-04-30 slice, so the main script keeps the prior `<= -5%` loss-weighting logic only.
- Expanded the current single-path backtest to 2026-03-01 through 2026-04-30. March was a broad weak segment for this first-layer Top3 model: 66 picks, `>=10%` hits 7, `<=-5%` losses 23, average return -1.43%. April had 63 picks, `>=10%` hits 21, `<=-5%` losses 6, average return 6.50%.
- Tested a candidate `failed_pullback_chase_risk_score` idea in-memory before adding code. Adding it as a 61st feature or replacing several lower-priority compact60 features all underperformed the current baseline on 2026-03-01 through 2026-04-30, so this feature is not added to the main code.
- Tested 5-level market opportunity gating in `scripts/03_apply_signal_decision_layer.py`: `A强机会 -> Top3`, `B偏强 -> Top2`, `C中性 -> Top1`, `D偏弱/E高风险 -> 不出手`. The classifier combined the market risk prediction with same-day market breadth, average market move, 5-day strong-stock ratio, and 5-day loss-spread ratio.
- On 2026-03-01 through 2026-04-30, the 5-level gate produced 8 final signals across 6 April dates and no March trades: `>=10%` hits 4/8, `>=5%` hits 5/8, `<=-5%` losses 0/8, average return 7.30%, median return 9.29%.
- Rejected the market gate as the production path because the core product requirement is stable daily Top3, not selective no-trade. Reworked `scripts/03_apply_signal_decision_layer.py` so the 5-level market classifier is diagnostic in the signal layer and no longer reduces the number of picks. Final selection is fixed daily Top3 from available candidates.
- Changed `scripts/01_predict_stock_direction.py` from daily Top3 final output to daily Top20 candidate-pool output. The final signal layer remains responsible for selecting exactly Top3, so market/industry/loss-risk adjustments can replace weak candidates instead of only reordering the same three stocks.
- User corrected the architecture: market risk should be learned by the stock model, not mainly applied as a post-model correction. Tested three leakage-safe market-prediction feature variants on 2026-03-01 through 2026-04-30, all using market predictions generated only from earlier known labels:
  - Replacing compact60 fields with handcrafted market-risk fields: Top3 `>=10%` fell from 28/129 to 14/129 and `<=-5%` losses rose from 29/129 to 33/129.
  - Adding market-prediction interaction fields on top of compact60: Top3 `>=10%` was 21/129, still worse than 28/129.
  - Adding only market predicted-down probability on top of compact60: Top3 `>=10%` was 23/129, still worse than 28/129.
- Conclusion: the direct market-risk-as-feature variants tested today are not production quality and were removed from the main stock model path. Keep the current compact60 baseline as the single production path until a market-aware model beats it.
- Tested market-state sample weighting on 2026-03-01 through 2026-04-30. Dates were classified from same-day market breadth/average move/5-day loss spread into `weak`, `neutral`, and `strong`; weak-market true winners and weak-market loss/fake-strong samples received higher training weights, while strong-market winners received higher weights. Result was worse than the compact60 baseline: Top3 `>=10%` was 24/129 vs baseline 28/129, and `<=-5%` losses were 32/129 vs baseline 29/129. This weighting logic was removed from the main path.
- Tested a second-layer Top20 reranker. On only 2026-03 through 2026-04 candidate history, LambdaRank produced Top3 `>=10%` 19/129 and `<=-5%` 36/129. After expanding candidate history to 2025-10 through 2026-04, LambdaRank still produced only 19/129 on 2026-03 through 2026-04. Additional quick tests with `>=10%` classification, future-return regression, and a hybrid base-score/classifier rerank peaked at 26/129 with 31/129 losses, still worse than the compact60 baseline 28/129 with 29/129 losses. The second-layer reranker script was removed from the main path.
- Tested first-layer objective refactors on 2026-03-01 through 2026-04-30 without changing the production script. Binary labels based on same-day future-return rank underperformed the compact60 baseline:
  - `daily_future_return_rank <= 3`: Top3 `>=10%` 18/129, `<=-5%` 30/129.
  - `rank <= 5` and `future_5_return >= 5%`: Top3 `>=10%` 19/129, `<=-5%` 26/129.
  - `rank <= 10` and `future_5_return >= 10%`: Top3 `>=10%` 12/129, `<=-5%` 27/129.
  - `rank <= 20` and `future_5_return >= 10%`: Top3 `>=10%` 15/129, `<=-5%` 28/129.
  - direct `future_5_return >= 10%` with rank-aware weights: Top3 `>=10%` 13/129, `<=-5%` 39/129.
- Also tested first-layer full-universe LambdaRank by trade date with relevance buckets from future 5-day return. Best variant produced Top3 `>=10%` 20/129 and `<=-5%` 31/129. Conclusion: ranking-target refactors tested today do not beat the current compact60 explosive-return baseline and were not added to the main path.
- Shifted to signal engineering from existing data instead of adding more model layers. Built composite signal probes from current/past stock, pool-internal theme, industry, and market fields: hot explosion, healthy breakout, repair quality, theme rotation, heat risk, and net quality.
- The single-signal probe on 2025-01 through 2026-05 showed a clear tradeoff: hot/explosion signals improve `>=10%` capture but also raise `<=-5%` losses; repair/relative-strength signals reduce losses and improve median return but do not catch enough 10% explosions.
- Tested market-state adaptive hand-built signal combinations. Training window was 2025-01-01 through 2026-02-28, validation was 2026-03-01 through 2026-04-30. The adaptive signal had 15/129 Top3 `>=10%` hits and 42/129 `<=-5%` losses on March-April, with March especially poor at 5/66 hits and 30/66 losses.
- Diagnostic best train-selected combinations could reach around 29/129 Top3 `>=10%` hits on March-April, but still carried about 39-41/129 `<=-5%` losses. This does not beat the current compact60 model path on risk-adjusted quality.
- Conclusion: do not replace the main Top3 selector with hand-built signal ranking. The useful next step is to add the few semantic blocks as model features and validate whether the stock model can learn when to prefer breakout versus low-position repair by market state.
- Selected and analyzed the worst March-April 2026 main-model dates in `outputs/failure_analysis_202603_202604_worst/`. The worst March dates were 2026-03-13, 03-06, 03-16, 03-11, 03-12, 03-10, 03-17, and 03-19. The worst April dates were 2026-04-08, 04-01, 04-29, 04-20, and 04-09.
- On these poor dates, Top3 failure tags concentrated in theme crowding, high 20-day return/high position, turnover crowding, and market loss-spread. Actual same-day winners were much more often theme/industry diffusion, low turnover, low-position repair, and pre-breakout quiet starts.
- Key split: March failures are mostly first-layer recall failures because many actual winners never entered Top20. April failures include both recall misses and sorting misses where real winners existed in Top20 but failed to reach Top3.
- Analyzed industry heat shapes on 2025-01 through 2026-05 labeled stock-pool samples in `outputs/industry_heat_shape_analysis_202501_202605/`.
- Industry heat shapes were built from same-day/past industry breadth, average return, industry-index trend/position, volume ratio, and industry loss-spread. Overall `future_5_return > 10%` base rate was 7.42% and `< -5%` base rate was 14.35%.
- Strong heat diffusion had the highest overall `>10%` rate at 10.12%, but also a high `<-5%` rate at 19.86%. High-position volume crowding had `>10%` 9.10% and `<-5%` 21.40%, so heat alone is not a clean buy signal.
- Weak/loss-spread industries still produced many `>10%` winners, consistent with oversold rebound opportunities, but the useful signal needs stock-level repair quality rather than just industry weakness.
- March 2026 had poor risk across nearly all heat shapes; April 2026 rewarded heat much more. Industry heat must be interacted with market state and stock crowding/position.
- Added cross analysis in `outputs/industry_market_stock_cross_analysis_202501_202605/` for market state × industry heat shape × stock position/crowding shape.
- High-value combinations with higher `>10%` and controlled `<-5%` were concentrated in repair/stronger markets with low-position repair or healthy-trend stocks, for example repair market + low-position industry repair + low-position stock repair had `>10%` 13.95% and `<-5%` 6.98%.
- The most dangerous recurring pattern is industry heat plus stock high-position crowding/end-stage. These combinations often have high `>10%` rates but `<=-5%` risk around 25%-40%, so they should become explicit fake-strong negative samples or penalty features.
- March/April split remained clear: March's apparent best combos still carried high loss rates, while April produced several high-hit/low-loss rebound combinations. This reinforces that market state should condition how industry heat and stock position are interpreted.
- Added stock cross-section shape analysis in `outputs/stock_cross_section_shape_analysis_202501_202605/`.
- Stock high-position crowding/end-stage had the highest `>10%` rate at 13.33%, but also the highest `<-5%` rate at 26.64%. It accounts for 21.59% of all `>10%` winners, so it cannot simply be removed, but it must be separated into clean continuation versus fake-strong terminal cases.
- Low-position quiet repair had low loss risk (`<-5%` 9.10%) but weak standalone `>10%` rate (4.16%), meaning it needs market/industry/theme confirmation to become a selection signal.
- Cross-section rank buckets show the same tradeoff: top 20% turnover rank has `>10%` 11.33% but `<-5%` 24.96%; top 20% 20-day return rank has `>10%` 12.68% but `<-5%` 24.03%.
- March 2026 was especially hostile to high-position crowding (`>10%` 11.73%, `<-5%` 45.06%), while April rewarded weak-break/rebound and high-crowding shapes much more. Market-state conditioning is required.
- Combined the stock cross-section shape with the earlier market state, industry heat shape, and stock position/crowding shape in `outputs/four_shape_combo_analysis_202501_202605/`.
- Four-dimensional theoretical combinations were 2,016. With min sample size 40, 191 combinations were retained, covering 52,272 rows or 92.07% of all samples; 411 small-sample combinations covered 7.93% of rows, and 1,414 combinations never occurred.
- High-value four-shape combinations were mostly non-terminal or repair/rebound states, e.g. weak-market loss-spread industry + neutral stock + market-strong leader had `>10%` 16.33% and `<-5%` 10.20%; repair-market neutral industry + neutral stock + low-volume repair had `>10%` 10.94% and `<-5%` 1.56%.
- High-opportunity/high-risk four-shape combinations were almost entirely stock high-position crowding/end-stage, often with `<-5%` around 24%-41%. This confirms high-position crowding should be modeled as a separate fake-strong risk family rather than used as a pure positive signal.
- Tested four-shape features in the stock model on 2026-03 through 2026-04 without changing the main path. Adding 34 one-hot/score shape features to compact60 produced Top3 `>=10%` 23/129 vs baseline 24/129, while `<=-5%` worsened from 29/129 to 39/129.
- Tested a smaller 9-score shape feature set. It produced Top3 `>=10%` 19/129 and `<=-5%` 35/129, also worse than baseline.
- Tested the 9-score feature set with additional sample weighting for fake-strong losses and clean positives. Losses returned to baseline at 29/129, but `>=10%` collapsed to 13/129. Conclusion: do not add four-shape features or the tested shape weighting to the production model.
- Current interpretation: the four-shape analysis is useful for diagnostics and failure-family design, but direct feature injection causes the model to overreact. The next viable route is not more features; it is either a cleaner target/ranking setup or a separate focused classifier for high-position fake-strong risk that only competes inside similar high-opportunity candidates.
- Reran the current fixed baseline on 2026-03-01 through 2026-05-31 into `outputs/baseline_202603_202605_rerun/`. Baseline config remained: 20% explosive-return label, `compact60`, LightGBM baseline, terminal-risk penalty, Top20 candidates, and Top3 selected by `final_return_signal_score`.
- 2026-03 Top3 result: 22 trading days, 66 verified picks, `>=10%` 7/66 (10.61%), `>=5%` 9/66 (13.64%), `<=-5%` 22/66 (33.33%), average 5-day return -1.56%, median -2.94%.
- 2026-04 Top3 result: 21 trading days, 63 verified picks, `>=10%` 17/63 (26.98%), `>=5%` 23/63 (36.51%), `<=-5%` 7/63 (11.11%), average 5-day return 4.18%, median 2.24%.
- 2026-05 Top3 result as of 2026-05-29: 15 predicted trading days, 45 picks, but only 30 picks have known 5-day labels. On known labels, `>=10%` 3/30 (10.00%), `>=5%` 5/30 (16.67%), `<=-5%` 13/30 (43.33%), average 5-day return -2.14%, median -3.57%. 2026-05-20 through 2026-05-26 remain pending label verification.
- Current conclusion from the 3-5月 rerun: April is the only month where the current baseline is close to usable; March and May known labels show the same weak-market failure family, with too many fake-strong/overheated losses and low `>=10%` capture.
- Tested whether the current baseline Top20 candidate pool itself has value versus random and simple strength rules on the 53 verified 2026-03 to 2026-05 dates. Output: `outputs/baseline_202603_202605_rerun/模型Top20_vs_随机和简单强势规则.md`.
- Result: model Top20 beats random on average return and `>=10%` capture (`0.44%` avg return and `12.64%` `>=10%` vs random expectation `0.18%` and `7.98%`), but its `<=-5%` loss rate is worse than random (`25.28%` vs `20.61%`).
- More importantly, the model Top20 loses clearly to a very simple `ret_20` strength rule: simple 20-day strength Top20 has `1.32%` average return and `18.30%` `>=10%`, versus model `0.44%` and `12.64%`. Therefore the current model has not proven enough candidate-pool edge; do not keep tuning parameters on this baseline. Next priority should be rebuilding labels, candidate pool, and feature definitions around why 20-day strength is capturing more explosive winners while controlling the false-strong loss family.
- Built the first candidate-pool rebuild probe in `outputs/candidate_pool_rebuild_probe_202603_202605/候选池重做证据_模型Top20_vs_20日强势.md`. On the same 53 verified dates, model Top20 and 20-day-strength Top20 overlap only 2.45 names per day on average, so the simple rule is finding a materially different opportunity set.
- Recall analysis for all `future_5_return >= 10%` stocks in the pool: model Top20 caught 134 winners (14.27% of all winners), raw 20-day-strength Top20 caught 194 winners (20.66%), and the set of winners missed by the model but caught by 20-day strength was 166 names (17.68%). The missed-by-model/caught-by-ret20 group had median `ret_20` around 29.93%, median `ret_5` 8.39%, median range position 0.86, and median terminal-chase score 0.60.
- Quick handcrafted attempts to reduce ret20 risk by subtracting terminal/crowding penalties underperformed raw 20-day strength: `20日强势质量Top20` had `>=10%` 139/1060 and avg 0.51%, while `20日强势平衡Top20` had `>=10%` 127/1060 and avg 0.38%, both below raw ret20 Top20 (`>=10%` 194/1060, avg 1.32%). Do not add these probe scores to production.
- Current candidate-pool conclusion: raw 20-day strength is a stronger recall mechanism than the current model, despite similar/high loss risk. The next design should use 20-day relative strength as an explicit candidate-pool backbone, then separately learn/define the fake-strong loss filter, instead of burying 20-day strength inside the current binary classifier.
- Built the requested 10-item baseline diagnostic table in `outputs/diagnostic_baseline_202603_202605/当前基线10项诊断表.md`. Scope: 2026-03 through 2026-05 verified labels only, 53 trading days.
- Diagnostic highlights: current model positive ratio (`future_5_return >= 20%`) was 2.34% in the first validation anchor's eligible training history and 1.51% in the validation pool. Full-pool validation average 5-day return was 0.18%, and full-pool `>=10%` rate was 8.00%.
- Current model Top20 had average 5-day return 0.44%, `>=10%` 134/1060 (12.64%), and `<=-5%` 268/1060 (25.28%). Model Top10 had average 0.59% and `>=10%` 72/530 (13.58%). Random Top20 expectation was 0.18% average return. Near-5-day-strength Top20 was 0.13% average return, `>=10%` 148/1060 (13.96%), and `<=-5%` 308/1060 (29.06%).
- AUC diagnostic: rolling train AUC for the current 20% label was extremely high at about 0.9977, but validation AUC on the full prediction pool was only 0.6916 for the 20% label and 0.6170 against the business `>=10%` label. This is strong evidence that the current setup fits the training history too well but does not transfer enough to the target month regime/business target.
- Score distribution diagnostic: full-prediction-pool 20% probability had p10 0.016, p50 0.079, p90 0.403, mean 0.148; Top20-truncated probability had p10 0.155, p50 0.417, p90 0.736, mean 0.421. Scores are spread, but high scores are not converting enough into robust Top20 profitability.
- Continued the diagnostic without changing model code in `outputs/diagnostic_next_step_202603_202605/下一步诊断_分数分层_TopK_标签.md`. The full-pool score deciles are not cleanly monotonic: the highest raw-probability decile had `>=10%` 14.13% and `<=-5%` 32.60%, while some middle deciles had better average returns. The model score has direction, but high scores also concentrate high downside.
- TopK diagnostic using full-pool raw probabilities showed raw model probability Top20 at avg 0.78%, `>=10%` 16.42%, and `<=-5%` 27.45%; raw 20-day strength Top20 remained stronger at avg 1.32%, `>=10%` 18.30%, and `<=-5%` 26.79%. This reinforces that candidate-pool recall should lean on explicit 20-day strength/relative strength.
- Label diagnostic: in validation, `future>=10%` had 8.00% positives, while the current `future>=20%` label had only 1.51% positives. `daily_rank<=20 & future>=10%` had 6.16% positives and directly targets “daily candidate quality + business return”. This is a better candidate label family to test next than pure 20% binary classification.
- Added a specific terminal-risk-penalty diagnostic in `outputs/diagnostic_next_step_202603_202605/风险扣分诊断_原始概率_vs_final_score.md`. Recomputing the same penalty on the full validation prediction pool: raw-probability Top20 had avg 0.78%, `>=10%` 16.42%, `<=-5%` 27.45%; penalty-adjusted final-score Top20 had avg 0.66%, `>=10%` 13.02%, `<=-5%` 24.62%.
- The risk penalty reduces losses but also removes too many winners: raw-probability Top20 names pushed out by the penalty had avg 1.44% and `>=10%` 21.95%, while new names introduced by the penalty had avg 0.98% and `>=10%` only 9.41%. Current conclusion: loss risk should not be a blunt first-ranking subtraction; it should be modeled or applied as a separate second-stage fake-strong filter only after a stronger candidate pool is built.
